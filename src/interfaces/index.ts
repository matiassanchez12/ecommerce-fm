export interface ISlideImage {
  url: string;
  thumbnail: string;
  caption: string;
}

export interface INavLinkItem {
  id: string;
  title: string;
}

export interface IProduct {
  name: string;
  quantity: number;
  price: number;
}
